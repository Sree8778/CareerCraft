import 'package:flutter/material.dart';

const Color primaryTextColor = Color(0xFF1F2937); // A dark gray for light mode
const Color mutedTextColor = Color(0xFF6B7280);   // A medium gray for light mode

const Color darkPrimaryTextColor = Color(0xFFF3F4F6); // A light gray for dark mode
const Color darkMutedTextColor = Color(0xFF9CA3AF);   // A gray for dark mode

const Color darkBackgroundFrom = Color(0xFF0A0A0A);   // From black
const Color darkBackgroundTo = Color(0xFF18181B);     // To zinc-900